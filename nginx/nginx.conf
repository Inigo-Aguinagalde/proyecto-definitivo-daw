events{}
http {

    upstream frontEnd {
        server frontEnd:1234;

    }

    upstream backend {
        server backend:1235;
 
    }
    server {
        listen  80;
        location / {
            proxy_pass http://frontEnd;
        }
        location /backend {
            proxy_pass http://backend;
        }
    }
}